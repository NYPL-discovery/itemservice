services:
  app:
    image: bref/php-83-fpm-dev:latest
    ports:
      - 8000:8000
    volumes:
      - .:/var/task
    env_file:
      - ./.env
      - ./config/local.env
    environment:
      HANDLER: index.php
      DOCUMENT_ROOT: .
      PHP_EXTENSION_DEBUG: 1
